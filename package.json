{
  "name": "the-boutique",
  "version": "1.0.0",
  "description": "Elegant shipping supplies & accessories store",
  "scripts": {
    "dev": "live-server --port=3000 --open=index.html --quiet",
    "start": "live-server --port=3000 --open=index.html --quiet"
  },
  "dependencies": {},
  "devDependencies": {
    "live-server": "^1.2.2"
  }
}
